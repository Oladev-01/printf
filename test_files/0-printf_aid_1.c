#include "main.h"
/**
 * _printf_aid_1 - this function checks if the argument
 * parsed is an integer and prints the integer(s)
 * or more directive(s) to the argument(s)
 *@num: this is the integer parsed
 * and as well subsequents aerguments
 * Return: count of the string
 */
int _printf_aid_1(int num)
{
	int count = 0, i = 0;

			printf("%d", num);
			count++;
	return (count);
}
